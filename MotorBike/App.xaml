<Application x:Class="MotorBike.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:MotorBike" ShutdownMode="OnMainWindowClose">
    <Application.Resources>

        <!--  ═══════════════════════════════════════════════════════════════  -->
        <!--  MotorBike Theme  ·  Refined Dark Slate  ·  Warm Orange Accent  -->
        <!--  ═══════════════════════════════════════════════════════════════  -->

        <!--  ═══ Menu Button Style ═══  -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#9BA4B5" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="HorizontalContentAlignment" Value="Right" />
            <Setter Property="Padding" Value="16,11" />
            <Setter Property="Margin" Value="4,1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Margin="8,1"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A3040" />
                                <Setter Property="Foreground" Value="#E8A54B" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#323848" />
                                <Setter Property="Foreground" Value="#F0B05E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#82B1FF"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Custom Box -->
                            <Border x:Name="CheckBorder"
                            Width="20" Height="20" CornerRadius="5"
                            Background="#2F3549" BorderBrush="#4A5270" BorderThickness="1.5"
                            VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBlock x:Name="CheckMark"
                                   Text="✔" FontSize="12" FontWeight="Bold"
                                   Foreground="#82B1FF"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                            </Border>
                            <!-- Label -->
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#1E2D4A"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#82B1FF"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#82B1FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBorder" Property="Opacity" Value="0.4"/>
                                <Setter Property="Foreground" Value="#4A5270"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!--  ═══ Primary Button (Warm Orange) ═══  -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E8A54B" />
            <Setter Property="Foreground" Value="#1E2233" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="GlowShadow"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8" Opacity="0.25">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="16" Direction="270" Opacity="0.4" ShadowDepth="0" Color="#E8A54B" />
                                </Border.Effect>
                            </Border>
                            <Border x:Name="border"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D4922F" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.4" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C07E1F" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.15" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#3D4560" />
                                <Setter Property="Foreground" Value="#6B7490" />
                                <Setter TargetName="GlowShadow" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═══ Danger Button ═══  -->
        <Style x:Key="DangerButton"
               BasedOn="{StaticResource PrimaryButton}"
               TargetType="Button">
            <Setter Property="Background" Value="#D9534F" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="GlowShadow"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8" Opacity="0.2">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" Direction="270" Opacity="0.35" ShadowDepth="0" Color="#D9534F" />
                                </Border.Effect>
                            </Border>
                            <Border x:Name="border"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C9433F" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.35" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B93330" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#3D4560" />
                                <Setter Property="Foreground" Value="#6B7490" />
                                <Setter TargetName="GlowShadow" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═══ Secondary Button ═══  -->
        <Style x:Key="SecondaryButton"
               BasedOn="{StaticResource PrimaryButton}"
               TargetType="Button">
            <Setter Property="Background" Value="#323848" />
            <Setter Property="Foreground" Value="#C8CEE0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="#424A5E" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D4560" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#E8A54B" />
                                <Setter Property="Foreground" Value="#ECEEF5" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A3040" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#282D3C" />
                                <Setter Property="Foreground" Value="#555D72" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#323848" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═══ Success Button ═══  -->
        <Style x:Key="SuccessButton"
               BasedOn="{StaticResource PrimaryButton}"
               TargetType="Button">
            <Setter Property="Background" Value="#5BAE7C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="GlowShadow"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8" Opacity="0.2">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" Direction="270" Opacity="0.35" ShadowDepth="0" Color="#5BAE7C" />
                                </Border.Effect>
                            </Border>
                            <Border x:Name="border"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4A9D6B" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.35" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D8C5C" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#3D4560" />
                                <Setter Property="Foreground" Value="#6B7490" />
                                <Setter TargetName="GlowShadow" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═══ Warning Button ═══  -->
        <Style x:Key="WarningButton"
               BasedOn="{StaticResource PrimaryButton}"
               TargetType="Button">
            <Setter Property="Background" Value="#E8A54B" />
            <Setter Property="Foreground" Value="#1E2233" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="GlowShadow"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8" Opacity="0.2">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" Direction="270" Opacity="0.35" ShadowDepth="0" Color="#E8A54B" />
                                </Border.Effect>
                            </Border>
                            <Border x:Name="border"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D4922F" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.35" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C07E1F" />
                                <Setter TargetName="GlowShadow" Property="Opacity" Value="0.1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#3D4560" />
                                <Setter Property="Foreground" Value="#6B7490" />
                                <Setter TargetName="GlowShadow" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E8A54B"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="0.5" ScaleY="0.5"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Margin="6" x:Name="RootGrid" RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Grid.RenderTransform>
                            <!-- Normal Shadow -->
                            <Border x:Name="NormalShadow" Background="White" CornerRadius="16">
                                <Border.Effect>
                                    <DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="8" ShadowDepth="2" Direction="270" />
                                </Border.Effect>
                            </Border>

                            <!-- Hover Shadow -->
                            <Border x:Name="HoverShadow" Background="White" CornerRadius="16" Opacity="0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#0F172A" Opacity="0.1" BlurRadius="20" ShadowDepth="6" Direction="270" />
                                </Border.Effect>
                            </Border>

                            <Border x:Name="CardBorder" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="16">
                                <Grid>
                                    <Border x:Name="HoverGlow" Background="#3B82F6" Opacity="0" CornerRadius="15"/>
                                    <Border x:Name="InnerBackground" Background="{TemplateBinding Background}" CornerRadius="15" Margin="1"/>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverShadow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="NormalShadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverShadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="NormalShadow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InnerBackground" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#93C5FD"/>
                                <Setter TargetName="HoverGlow" Property="Opacity" Value="0.05"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="InnerBackground" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.8">
                                <DoubleAnimation.EasingFunction>
                                    <ElasticEase Oscillations="1" Springiness="4" EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.8">
                                <DoubleAnimation.EasingFunction>
                                    <ElasticEase Oscillations="1" Springiness="4" EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>


        <!--  ═══ Form TextBox ═══  -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#ECEEF5" />
            <Setter Property="CaretBrush" Value="#E8A54B" />
            <Setter Property="BorderBrush" Value="#3D4560" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#272C3F" />
            <Setter Property="SelectionBrush" Value="#E8A54B" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E8A54B" />
                                <Setter TargetName="border" Property="Background" Value="#2F3549" />
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A5270" />
                                <Setter TargetName="border" Property="Background" Value="#2C3245" />
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222738" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#343A50" />
                                <Setter Property="Foreground" Value="#9BA4B5" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#222738" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#303650" />
                                <Setter Property="Foreground" Value="#6B7490" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═══ Form Label ═══  -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <!--  ═══ Toggle Switch ═══  -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Background" Value="#E8A54B" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel FlowDirection="LeftToRight" Orientation="Horizontal">
                            <Grid Width="36" Height="20" Background="Transparent">
                                <Border x:Name="Track" Width="36" Height="18" VerticalAlignment="Center" Background="#424A5E"
                                        CornerRadius="9" />
                                <Ellipse x:Name="Thumb" Width="14" Height="14" Margin="2,0,0,0" HorizontalAlignment="Left"
                                         VerticalAlignment="Center" Fill="White">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" Direction="270" Opacity="0.3" ShadowDepth="1" Color="#000000" />
                                    </Ellipse.Effect>
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform" X="0" Y="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>
                            <ContentPresenter Margin="8,0,8,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="18" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard x:Name="ExitStoryboard">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="Track" Property="Background" Value="#424A5E" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═════════════════════════════════════════════════  -->
        <!--  DARK COMBOBOX  ·  Toggle Button for dropdown  -->
        <!--  ═════════════════════════════════════════════════  -->
        <ControlTemplate x:Key="DarkComboBoxToggle" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="28" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" Background="#272C3F" BorderBrush="#3D4560" BorderThickness="1"
                        CornerRadius="8" />
                <Border x:Name="ArrowBg" Grid.Column="1" Background="Transparent" CornerRadius="0,8,8,0" />
                <Path x:Name="Arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 5 5 L 10 0 Z"
                      Fill="#9BA4B5" />
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#4A5270" />
                    <Setter TargetName="Arrow" Property="Fill" Value="#E8A54B" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#E8A54B" />
                    <Setter TargetName="Border" Property="BorderThickness" Value="1.5" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="Border" Property="Background" Value="#222738" />
                    <Setter TargetName="Border" Property="BorderBrush" Value="#303650" />
                    <Setter TargetName="Arrow" Property="Fill" Value="#555D72" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!--  ComboBox Editable TextBox  -->
        <ControlTemplate x:Key="DarkComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Background="Transparent" Focusable="False" />
        </ControlTemplate>

        <!--  ═══ Global Dark ComboBox (Implicit) ═══  -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#ECEEF5" />
            <Setter Property="Background" Value="#272C3F" />
            <Setter Property="BorderBrush" Value="#3D4560" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" ClickMode="Press" Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Template="{StaticResource DarkComboBoxToggle}" />
                            <ContentPresenter x:Name="ContentSite" Margin="12,8,28,8" HorizontalAlignment="Right" VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              IsHitTestVisible="False" />
                            <TextBox x:Name="PART_EditableTextBox" Margin="12,8,28,8" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Transparent"
                                     CaretBrush="#E8A54B" Focusable="True" Foreground="#ECEEF5"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"
                                     Style="{x:Null}"
                                     Template="{StaticResource DarkComboBoxTextBox}"
                                     Visibility="Hidden" />
                            <Popup x:Name="Popup" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      SnapsToDevicePixels="True">
                                    <Border x:Name="DropDownBorder" Margin="0,2,0,0" Background="#2F3549" BorderBrush="#4A5270" BorderThickness="1"
                                            CornerRadius="8">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="12" Direction="270" Opacity="0.2" ShadowDepth="4" Color="#000000" />
                                        </Border.Effect>
                                    </Border>
                                    <ScrollViewer Margin="4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="50" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter Property="IsTabStop" Value="False" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═══ ComboBoxItem (Dark) ═══  -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#C8CEE0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Margin="2,1"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#323F58" />
                                <Setter Property="Foreground" Value="#E8A54B" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3D4560" />
                                <Setter Property="Foreground" Value="#E8A54B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker Style -->
        <Style TargetType="DatePicker">
            <Setter Property="Foreground" Value="#C8CEE0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="Root" Background="#2F3549" BorderBrush="#4A5270"
                        BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="36"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox"
                                           Grid.Column="0" Padding="10,0"
                                           Background="Transparent"
                                           Foreground="#C8CEE0"
                                           BorderThickness="0"
                                           VerticalAlignment="Center"
                                           CaretBrush="#E8A54B"/>
                                <Button x:Name="PART_Button" Grid.Column="1"
                                Background="Transparent" BorderThickness="0"
                                VerticalAlignment="Center" HorizontalAlignment="Center"
                                Focusable="False">
                                    <TextBlock Text="📅" FontSize="15"/>
                                </Button>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True"
                               Placement="Bottom" StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#E8A54B"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#E8A54B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Calendar Style -->
        <Style TargetType="Calendar">
            <Setter Property="Background" Value="#272C3F"/>
            <Setter Property="BorderBrush" Value="#3D4560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#C8CEE0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Calendar">
                        <Border Background="#272C3F" BorderBrush="#3D4560"
                        BorderThickness="1" CornerRadius="12" Padding="10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" Direction="270"
                                          Opacity="0.3" ShadowDepth="4" Color="#000000"/>
                            </Border.Effect>
                            <CalendarItem x:Name="PART_CalendarItem"
                                  Background="#272C3F"
                                  BorderBrush="Transparent"
                                  BorderThickness="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CalendarItem Style -->
        <Style TargetType="CalendarItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Header: Prev / Title / Next -->
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="PART_PreviousButton" Grid.Column="0"
                                Background="Transparent" BorderThickness="0"
                                Foreground="#E8A54B" FontSize="16"
                                Content="‹" Cursor="Hand"/>
                                <Button x:Name="PART_HeaderButton" Grid.Column="1"
                                Background="Transparent" BorderThickness="0"
                                Foreground="#ECEEF5" FontWeight="Bold" FontSize="13"
                                HorizontalAlignment="Center" Cursor="Hand"/>
                                <Button x:Name="PART_NextButton" Grid.Column="2"
                                Background="Transparent" BorderThickness="0"
                                Foreground="#E8A54B" FontSize="16"
                                Content="›" Cursor="Hand"/>
                            </Grid>

                            <!-- Day Names Row -->
                            <Grid x:Name="PART_MonthView" Grid.Row="1"/>
                            <Grid x:Name="PART_YearView" Grid.Row="1" Visibility="Hidden"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Calendar Day Button -->
        <Style TargetType="CalendarDayButton">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#C8CEE0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Border x:Name="DayBorder" Background="{TemplateBinding Background}"
                        CornerRadius="16" Width="30" Height="30">
                            <TextBlock x:Name="DayText" Text="{TemplateBinding Content}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{TemplateBinding Foreground}" FontSize="12"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="#323F58"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="#E8A54B"/>
                                <Setter TargetName="DayText" Property="Foreground" Value="#1E2233"/>
                                <Setter TargetName="DayText" Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter TargetName="DayBorder" Property="BorderBrush" Value="#E8A54B"/>
                                <Setter TargetName="DayBorder" Property="BorderThickness" Value="1.5"/>
                                <Setter TargetName="DayText" Property="Foreground" Value="#E8A54B"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter TargetName="DayText" Property="Foreground" Value="#4A5270"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Calendar Button (Month/Year view) -->
        <Style TargetType="CalendarButton">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#C8CEE0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarButton">
                        <Border x:Name="CalBtnBorder" Background="{TemplateBinding Background}"
                        CornerRadius="8">
                            <TextBlock Text="{TemplateBinding Content}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{TemplateBinding Foreground}" FontSize="12"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CalBtnBorder" Property="Background" Value="#323F58"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePickerTextBox -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#C8CEE0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid>
                            <TextBlock x:Name="PART_Watermark"
                               Text="اختر التاريخ" Foreground="#4A5270"
                               VerticalAlignment="Center" Margin="2,0"
                               Visibility="Collapsed"/>
                            <ScrollViewer x:Name="PART_ContentHost"
                                  VerticalAlignment="Center"
                                  Foreground="#C8CEE0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ═════════════════════════════════════════  -->
        <!--  DARK SCROLLBAR  -->
        <!--  ═════════════════════════════════════════  -->
        <Style x:Key="DarkScrollBarThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder" Margin="1" Background="#3D4560" CornerRadius="4" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#4A5270" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#E8A54B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="10" />
            <Setter Property="MinWidth" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="#1E2233">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DarkScrollBarThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="10" />
                    <Setter Property="MinHeight" Value="10" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="#1E2233">
                                    <Track x:Name="PART_Track" IsDirectionReversed="False">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource DarkScrollBarThumb}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--  ═══ Dark ToolTip ═══  -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#2F3549" />
            <Setter Property="Foreground" Value="#ECEEF5" />
            <Setter Property="BorderBrush" Value="#4A5270" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="3" Color="#000000" />
                            </Border.Effect>
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
