<UserControl x:Class="MotorBike.Views.ItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MotorBike.Converters"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Loaded="OnLoaded" FlowDirection="RightToLeft">
    <UserControl.Resources>
        <conv:HideZeroConverter x:Key="HideZeroConv"/>
        <conv:HideZeroConverter x:Key="HideZeroConverter" />
        <conv:IdToNameConverter x:Key="IdToNameConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="White" CornerRadius="12" Margin="0,0,0,16" BorderBrush="#E2E8F0" BorderThickness="1" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="12" ShadowDepth="4" Direction="270" />
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" RowHeight="44"
                      ColumnHeaderHeight="48" AlternatingRowBackground="#F8FAFC" Background="White" Foreground="#1E293B" FontSize="14" HorizontalGridLinesBrush="#F1F5F9">
                <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#F8FAFC"/><Setter Property="Foreground" Value="#475569"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderBrush" Value="#E2E8F0"/><Setter Property="BorderThickness" Value="0,0,0,1"/></Style></DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="Cursor" Value="Hand"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#DBEAFE"/><Setter Property="Foreground" Value="#1D4ED8"/></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#EFF6FF"/></Trigger></Style.Triggers></Style></DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الكود" Binding="{Binding ItemId}" Width="80"/>
                    <DataGridTextColumn Header="اسم الصنف" Binding="{Binding ItemName}" Width="*"/>
                    <DataGridTemplateColumn Header="المجموعة" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center" Padding="4,0">
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource IdToNameConverter}" ConverterParameter="CatId,CatName">
                                            <Binding Path="CatId" />
                                            <Binding Path="DataContext.Categories" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="الوحدة الصغرى" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center" Padding="4,0">
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource IdToNameConverter}" ConverterParameter="UnitId,UnitName">
                                            <Binding Path="UnitId" />
                                            <Binding Path="DataContext.Units" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="الباركود" Binding="{Binding Bar1}" Width="150"/>
                    <DataGridTextColumn Header="سعر الشراء" Binding="{Binding Price0, Converter={StaticResource HideZeroConverter}}" Width="110"/>
                    <DataGridTextColumn Header="سعر البيع" Binding="{Binding Price1, Converter={StaticResource HideZeroConverter}}" Width="110"/>
                    <DataGridTemplateColumn Header="نشط" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Active, Mode=OneWay}" Style="{StaticResource ToggleSwitchStyle}" IsEnabled="False" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="12" ShadowDepth="4" Direction="270" />
            </Border.Effect>
            <StackPanel>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                    <Grid Margin="0,0,0,24" IsEnabled="{Binding IsEditing}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/> <ColumnDefinition Width="150"/> <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="100"/> <ColumnDefinition Width="150"/> <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="100"/> <ColumnDefinition Width="150"/> <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="100"/> <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/> <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/> <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/> <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/> <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0: ID, Name, CatId -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="الكود" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FormItem.ItemId, Converter={StaticResource HideZeroConv}}" Style="{StaticResource FormTextBox}" IsReadOnly="True"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="3" Text="اسم الصنف" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="4" Text="{Binding FormItem.ItemName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Grid.Row="0" Grid.Column="6" Text="المجموعة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="0" Grid.Column="7" 
                                  ItemsSource="{Binding Categories}" 
                                  DisplayMemberPath="CatName" 
                                  SelectedValuePath="CatId" 
                                  SelectedValue="{Binding FormItem.CatId, UpdateSourceTrigger=PropertyChanged}" 
                                  Style="{DynamicResource {x:Type ComboBox}}" 
                                  VerticalContentAlignment="Center"
                                  Padding="8,0"
                                  BorderBrush="#CBD5E1" 
                                  BorderThickness="1" />

                        <CheckBox Style="{StaticResource ToggleSwitchStyle}" Grid.Row="0" Grid.Column="9" Grid.ColumnSpan="2" Content="نشط" IsChecked="{Binding FormItem.Active}" VerticalAlignment="Center" FontSize="14"/>

                        <!-- Row 2: Price0, DiscPer0, Price1 -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="سعر الشراء" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FormItem.Price0, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="3" Text="خصم الشراء (%)" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding FormItem.DiscPer0, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Grid.Row="2" Grid.Column="6" Text="متوسط التكلفة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="7" Text="{Binding FormItem.AvrgCost, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}" IsReadOnly="True"/>

                        <TextBlock Grid.Row="2" Grid.Column="9" Text="سعر الاستيراد" Style="{StaticResource FormLabel}" VerticalAlignment="Center" Foreground="#1D4ED8"/>
                        <TextBox Grid.Row="2" Grid.Column="10" Text="{Binding FormItem.ImpPrice, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}" BorderBrush="#1D4ED8"/>

                        <!-- Row 4: Price1, DiscPer1, MinPrice -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="سعر البيع" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding FormItem.Price1, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}"/>
                        
                        <TextBlock Grid.Row="4" Grid.Column="3" Text="خصم البيع (%)" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="4" Text="{Binding FormItem.DiscPer1, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Grid.Row="4" Grid.Column="6" Text="أقل سعر بيع" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="7" Text="{Binding FormItem.MinPrice, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}"/>

                        <!-- Row 6: Units -->
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="الوحدة الصغرى" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="6" Grid.Column="1" 
                                  ItemsSource="{Binding Units}" 
                                  DisplayMemberPath="UnitName" 
                                  SelectedValuePath="UnitId" 
                                  SelectedValue="{Binding FormItem.UnitId, UpdateSourceTrigger=PropertyChanged}" 
                                  Style="{DynamicResource {x:Type ComboBox}}" 
                                  VerticalContentAlignment="Center"
                                  Padding="8,0"
                                  BorderBrush="#CBD5E1" 
                                  BorderThickness="1" />

                        <TextBlock Grid.Row="6" Grid.Column="3" Text="الوحدة الكبرى" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="6" Grid.Column="4" 
                                  ItemsSource="{Binding Units}" 
                                  DisplayMemberPath="UnitName" 
                                  SelectedValuePath="UnitId" 
                                  SelectedValue="{Binding FormItem.Unit2, UpdateSourceTrigger=PropertyChanged}" 
                                  Style="{DynamicResource {x:Type ComboBox}}" 
                                  VerticalContentAlignment="Center"
                                  Padding="8,0"
                                  BorderBrush="#CBD5E1" 
                                  BorderThickness="1" />

                        <TextBlock Grid.Row="6" Grid.Column="6" Text="عدد الصغرى بالكبرى" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="6" Grid.Column="7" Text="{Binding FormItem.Unit2Qty, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}"/>

                        <!-- Row 8: Barcodes -->
                        <TextBlock Grid.Row="8" Grid.Column="0" Text="الباركود (1)" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding FormItem.Bar1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Grid.Row="8" Grid.Column="3" Text="الباركود (2)" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="8" Grid.Column="4" Text="{Binding FormItem.Bar2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Grid.Row="8" Grid.Column="6" Text="ملاحظات" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="8" Grid.Column="7" Text="{Binding FormItem.Notes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                        <!-- Row 10: IsLimit, Limit, IsStock -->
                        <CheckBox Style="{StaticResource ToggleSwitchStyle}" Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="2" Content="تفعيل حد الطلب" IsChecked="{Binding FormItem.IsLimit}" VerticalAlignment="Center" FontSize="14"/>

                        <TextBlock Grid.Row="10" Grid.Column="3" Text="الحد الأدنى" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="10" Grid.Column="4" Text="{Binding FormItem.Limit, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HideZeroConverter}}" Style="{StaticResource FormTextBox}" IsEnabled="{Binding FormItem.IsLimit}"/>

                        <CheckBox Style="{StaticResource ToggleSwitchStyle}" Grid.Row="10" Grid.Column="6" Grid.ColumnSpan="2" Content="صنف مخزني" IsChecked="{Binding FormItem.IsStock}" VerticalAlignment="Center" FontSize="14"/>
                    </Grid>
                </ScrollViewer>
                
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Content="➕ جديد" Style="{StaticResource PrimaryButton}" Command="{Binding AddNewCommand}"/>
                        <Button Content="✏️ تعديل" Style="{StaticResource SecondaryButton}" Command="{Binding EditSelectedCommand}"/>
                        <Button Content="💾 حفظ" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" IsEnabled="{Binding IsEditing}"/>
                        <Button Content="🗑️ حذف" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}"/>
                        <Button Content="✖ إلغاء" Style="{StaticResource SecondaryButton}" Command="{Binding CancelEditCommand}" IsEnabled="{Binding IsEditing}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#10B981" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="24,0,0,0"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
