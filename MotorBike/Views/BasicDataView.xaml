<UserControl x:Class="MotorBike.Views.BasicDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <!-- A modern button style for the cards -->
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Margin="6">
                            <!-- Normal Shadow -->
                            <Border x:Name="NormalShadow" Background="White" CornerRadius="16">
                                <Border.Effect>
                                    <DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="8" ShadowDepth="2" Direction="270" />
                                </Border.Effect>
                            </Border>
                            
                            <!-- Hover Shadow -->
                            <Border x:Name="HoverShadow" Background="White" CornerRadius="16" Opacity="0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#0F172A" Opacity="0.08" BlurRadius="16" ShadowDepth="4" Direction="270" />
                                </Border.Effect>
                            </Border>

                            <Border x:Name="CardBorder" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="16">
                                <Grid>
                                    <!-- Optional hover background glow -->
                                    <Border x:Name="HoverGlow" Background="#3B82F6" Opacity="0" CornerRadius="15"/>
                                    <Border x:Name="InnerBackground" Background="{TemplateBinding Background}" CornerRadius="15" Margin="1"/>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InnerBackground" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#93C5FD"/>
                                <Setter TargetName="HoverGlow" Property="Opacity" Value="0.1"/>
                                <Setter TargetName="NormalShadow" Property="Opacity" Value="0"/>
                                <Setter TargetName="HoverShadow" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="InnerBackground" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="HoverShadow" Property="Opacity" Value="0"/>
                                <Setter TargetName="NormalShadow" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Style for Category Header -->
        <Style x:Key="CategoryHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Margin" Value="0,20,0,15"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- Style for the Icon Circle -->
        <Style x:Key="IconBorderStyle" TargetType="Border">
            <Setter Property="Width" Value="64"/>
            <Setter Property="Height" Value="64"/>
            <Setter Property="CornerRadius" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
        
        <!-- Style for the Icon Text -->
        <Style x:Key="IconTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- Style for the Title Text -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F8FAFC">
        <StackPanel Margin="40">
            <!-- Header section -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="البيانات الأساسية" FontSize="32" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                <TextBlock Text="إدارة كافة البيانات الأساسية للنظام من مكان واحد." FontSize="16" Foreground="#64748B"/>
            </StackPanel>

            <!-- بيانات عامة -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <TextBlock Text="📄" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="بيانات عامة" Style="{StaticResource CategoryHeaderStyle}" Margin="0"/>
            </StackPanel>
            <WrapPanel Margin="0,0,0,30">
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Company" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#EFF6FF">
                            <TextBlock Text="🏢" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="بيانات الشركة" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Users" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#F5F3FF">
                            <TextBlock Text="👤" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="المستخدمين" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Cities" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#ECFEFF">
                            <TextBlock Text="🌆" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="المدن" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
            </WrapPanel>

            <!-- المخازن والأصناف -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <TextBlock Text="📦" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="المخازن والأصناف" Style="{StaticResource CategoryHeaderStyle}" Margin="0"/>
            </StackPanel>
            <WrapPanel Margin="0,0,0,30">
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Units" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#F0FDF4">
                            <TextBlock Text="📏" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="وحدات الأصناف" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="ItemCategories" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#F0FDF4">
                            <TextBlock Text="📂" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="مجموعات الأصناف" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Stores" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#F0FDF4">
                            <TextBlock Text="🏪" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="المخازن" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
            </WrapPanel>

            <!-- بيانات المركبات -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <TextBlock Text="🏍️" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="بيانات المركبات" Style="{StaticResource CategoryHeaderStyle}" Margin="0"/>
            </StackPanel>
            <WrapPanel Margin="0,0,0,30">
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="CarBrands" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#FFFBEB">
                            <TextBlock Text="🏭" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="ماركات الموتوسيكلات" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="CarModels" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#FFFBEB">
                            <TextBlock Text="🏍️" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="موديلات الماركة" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Colors" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#FFFBEB">
                            <TextBlock Text="🎨" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="الألوان" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
            </WrapPanel>

            <!-- البيانات المالية -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <TextBlock Text="💵" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="البيانات المالية" Style="{StaticResource CategoryHeaderStyle}" Margin="0"/>
            </StackPanel>
            <WrapPanel Margin="0,0,0,30">
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Cash" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#FEF2F2">
                            <TextBlock Text="💰" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="الخزائن" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Omla" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#FEF2F2">
                            <TextBlock Text="💱" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="العملات" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="ExpGroups" Style="{StaticResource CardButtonStyle}" Width="210" Height="170" Margin="0,0,20,20">
                    <StackPanel VerticalAlignment="Center">
                        <Border Style="{StaticResource IconBorderStyle}" Background="#FEF2F2">
                            <TextBlock Text="📋" Style="{StaticResource IconTextStyle}"/>
                        </Border>
                        <TextBlock Text="مجموعات المصروفات" Style="{StaticResource TitleTextStyle}"/>
                    </StackPanel>
                </Button>
            </WrapPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
