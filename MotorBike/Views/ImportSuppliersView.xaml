<UserControl x:Class="MotorBike.Views.ImportSuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Loaded="OnLoaded" FlowDirection="RightToLeft">
    <Grid MinWidth="800" MinHeight="500" Background="#1E2233">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24,24,24,16">
            <StackPanel Orientation="Horizontal">
                <Border Background="#3F2259" CornerRadius="12" Padding="14,12" Margin="0,0,16,0">
                    <TextBlock Text="🌍" FontSize="26" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="موردين الاستيراد" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="إدارة الموردين الخارجيين وحساباتهم" FontSize="14" Foreground="#9BA4B5" Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- DataGrid Card (Master) -->
        <Border Grid.Row="1" Background="#272C3F" CornerRadius="12" Margin="24,0,24,16" BorderBrush="#3D4560" BorderThickness="1" ClipToBounds="True">
            <Border.Effect><DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="12" ShadowDepth="4" Direction="270"/></Border.Effect>
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" RowHeight="44"
                      ColumnHeaderHeight="48" AlternatingRowBackground="#222738" Background="#242940" Foreground="#C8CEE0" FontSize="13" HorizontalGridLinesBrush="#323848">
                
                
                <DataGrid.ColumnHeaderStyle>
                                    <Style BasedOn="{StaticResource DarkColumnHeader}" TargetType="DataGridColumnHeader" />
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.RowStyle>
                                    <Style BasedOn="{StaticResource DarkRow}" TargetType="DataGridRow" />
                                </DataGrid.RowStyle>
                                <DataGrid.CellStyle>
                                    <Style BasedOn="{StaticResource DarkCell}" TargetType="DataGridCell" />
                                </DataGrid.CellStyle>
                                <DataGrid.Columns>
                    <DataGridTextColumn Header="الكود" Binding="{Binding SuppId}" Width="80"/>
                    <DataGridTextColumn Header="اسم المورد" Binding="{Binding SuppName}" Width="2*"/>
                    <DataGridTextColumn Header="الدولة" Binding="{Binding Country}" Width="*"/>
                    <DataGridTextColumn Header="التليفون" Binding="{Binding Tel}" Width="*"/>
                    <DataGridTextColumn Header="الرصيد" Binding="{Binding Bal}" Width="100"/>
                    <DataGridTemplateColumn Header="نشط" Width="80"><DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <CheckBox IsChecked="{Binding Active, Mode=OneWay}" Style="{StaticResource ToggleSwitchStyle}" IsEnabled="False" HorizontalAlignment="Center"/>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Form Card (Detail) -->
        <Border Grid.Row="2" Background="#272C3F" CornerRadius="12" Padding="24" Margin="24,0,24,24" BorderBrush="#3D4560" BorderThickness="1">
            <Border.Effect><DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="12" ShadowDepth="4" Direction="270"/></Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,24" IsEnabled="{Binding IsEditing}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="الكود" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FormItem.SuppId}" Style="{StaticResource FormTextBox}" IsReadOnly="True" Background="#2A3040" Foreground="#ECEEF5"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="اسم المورد" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="4" Text="{Binding FormItem.SuppName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                    <TextBlock Grid.Row="0" Grid.Column="6" Text="الدولة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="7" Text="{Binding FormItem.Country, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="التليفون" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FormItem.Tel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="العنوان" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding FormItem.Adress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                    <TextBlock Grid.Row="2" Grid.Column="6" Text="العملة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="2" Grid.Column="7" ItemsSource="{Binding Omlas}" DisplayMemberPath="OmlaName" SelectedValuePath="OmlaId" SelectedValue="{Binding SelectedOmlaId}" Padding="10,6" FontSize="14"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="سعر الصرف" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding FormItem.OmlaRate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                    <TextBlock Grid.Row="4" Grid.Column="6" Text="تاريخ التعامل" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <DatePicker Grid.Row="4" Grid.Column="7" SelectedDate="{Binding FormItem.OpenDate}" Padding="10,4" FontSize="14"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="مدين افتتاحي" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtDebit" Grid.Row="6" Grid.Column="1" Text="{Binding FormItem.Debit, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}" TextChanged="txtDebitCredit_TextChanged"/>

                    <TextBlock Grid.Row="6" Grid.Column="3" Text="دائن افتتاحي" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtCredit" Grid.Row="6" Grid.Column="4" Text="{Binding FormItem.Credit, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}" TextChanged="txtDebitCredit_TextChanged"/>

                    <TextBlock Grid.Row="6" Grid.Column="6" Text="الرصيد" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <DockPanel Grid.Row="6" Grid.Column="7">
                        <CheckBox DockPanel.Dock="Right" Margin="16,0,0,0" Style="{StaticResource ToggleSwitchStyle}" Content="نشط" IsChecked="{Binding FormItem.Active}" VerticalAlignment="Center" FontSize="14"/>
                        <TextBox x:Name="txtBal" Text="{Binding FormItem.Bal, Mode=OneWay}" Style="{StaticResource FormTextBox}" IsReadOnly="True" Background="#F1F5F9" Foreground="#1D4ED8" FontWeight="Bold"/>
                    </DockPanel>
                    
                    <TextBlock Grid.Row="4" Grid.Column="3" Text="الملاحظات" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="4" Text="{Binding FormItem.Notes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                </Grid>

                <DockPanel>
                    <WrapPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Content="➕ جديد" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Command="{Binding AddNewCommand}"/>
                        <Button Content="✏️ تعديل" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Command="{Binding EditSelectedCommand}"/>
                        <Button Content="💾 حفظ" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Command="{Binding SaveCommand}" IsEnabled="{Binding IsEditing}"/>
                        <Button Content="🗑️ حذف" Style="{StaticResource DangerButton}" Margin="0,0,8,8" Command="{Binding DeleteCommand}"/>
                        <Button Content="✖ إلغاء" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Command="{Binding CancelEditCommand}" IsEnabled="{Binding IsEditing}"/>
                    </WrapPanel>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#10B981" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="24,0,0,0"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
