<UserControl x:Class="MotorBike.Views.ImportPaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MotorBike.Converters"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Loaded="OnLoaded" FlowDirection="RightToLeft">
    <UserControl.Resources>
        <conv:IdToNameConverter x:Key="IdToNameConverter"/>
    </UserControl.Resources>

    <Grid MinWidth="800" MinHeight="500" Background="#1E2233">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24,24,24,16">
            <StackPanel Orientation="Horizontal">
                <Border Background="#6B5300" CornerRadius="12" Padding="14,12" Margin="0,0,16,0">
                    <TextBlock Text="💰" FontSize="26" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="مدفوعات الاستيراد" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="إدارة الدفعات المسددة واختيار الفواتير الخاصة بها" FontSize="14" Foreground="#9BA4B5" Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- DataGrid Card (Master) -->
        <Border Grid.Row="1" Background="#272C3F" CornerRadius="12" Margin="24,0,24,16" BorderBrush="#3D4560" BorderThickness="1" ClipToBounds="True">
            <Border.Effect><DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="12" ShadowDepth="4" Direction="270"/></Border.Effect>
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" RowHeight="44"
                      ColumnHeaderHeight="48" AlternatingRowBackground="#222738" Background="#242940" Foreground="#C8CEE0" FontSize="13" HorizontalGridLinesBrush="#323848">
                
                
                <DataGrid.ColumnHeaderStyle>
                                    <Style BasedOn="{StaticResource DarkColumnHeader}" TargetType="DataGridColumnHeader" />
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.RowStyle>
                                    <Style BasedOn="{StaticResource DarkRow}" TargetType="DataGridRow" />
                                </DataGrid.RowStyle>
                                <DataGrid.CellStyle>
                                    <Style BasedOn="{StaticResource DarkCell}" TargetType="DataGridCell" />
                                </DataGrid.CellStyle>
                                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم الدفعة" Binding="{Binding PayId}" Width="100"/>
                    <DataGridTextColumn Header="تاريخ الدفع" Binding="{Binding PayDate, StringFormat=\{0:yyyy/MM/dd\}}" Width="100"/>
                    <DataGridTemplateColumn Header="المورد" Width="200"><DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <TextBlock VerticalAlignment="Center" Padding="4,0"><TextBlock.Text>
                            <MultiBinding Converter="{StaticResource IdToNameConverter}" ConverterParameter="SuppId,SuppName">
                                <Binding Path="SuppId"/><Binding Path="DataContext.Suppliers" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding></TextBlock.Text></TextBlock>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="الخزنة" Width="150"><DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <TextBlock VerticalAlignment="Center" Padding="4,0"><TextBlock.Text>
                            <MultiBinding Converter="{StaticResource IdToNameConverter}" ConverterParameter="CashId,CashName">
                                <Binding Path="CashId"/><Binding Path="DataContext.CashList" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding></TextBlock.Text></TextBlock>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                    <DataGridTextColumn Header="المبلغ" Binding="{Binding PayMoney, StringFormat=\{0:N2\}}" Width="120" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="الفاتورة (إن وجدت)" Binding="{Binding InvId}" Width="150"/>
                    <DataGridTextColumn Header="الملاحظات" Binding="{Binding Notes}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Form Card (Detail) -->
        <Border Grid.Row="2" Background="#272C3F" CornerRadius="12" Padding="24" Margin="24,0,24,24" BorderBrush="#3D4560" BorderThickness="1">
            <Border.Effect><DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="12" ShadowDepth="4" Direction="270"/></Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,24" IsEnabled="{Binding IsEditing}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="رقم الدفعة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FormItem.PayId}" Style="{StaticResource FormTextBox}" IsReadOnly="True" Background="#2A3040" Foreground="#ECEEF5"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="تاريخ الدفع" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <DatePicker Grid.Row="0" Grid.Column="4" SelectedDate="{Binding FormItem.PayDate}" Padding="10,4" FontSize="14"/>
                    <TextBlock Grid.Row="0" Grid.Column="6" Text="المبلغ المدفوع" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="7" Text="{Binding FormItem.PayMoney, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="المورد" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" VerticalAlignment="Center">
                        <ComboBox ItemsSource="{Binding Suppliers}" DisplayMemberPath="SuppName" SelectedItem="{Binding SelectedSupplier}" Padding="10,6" FontSize="14" IsEditable="True"/>
                        <TextBlock Margin="0,4,0,0" FontSize="12" FontWeight="SemiBold" Foreground="#E8A54B" Text="{Binding CurrentSupplierBalance, StringFormat='رصيد المورد: {0:N2}'}" />
                    </StackPanel>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="الخزنة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="2" Grid.Column="4" VerticalAlignment="Center">
                        <ComboBox ItemsSource="{Binding CashList}" DisplayMemberPath="CashName" SelectedValuePath="CashId" SelectedValue="{Binding SelectedCashId, UpdateSourceTrigger=PropertyChanged}" Padding="10,6" FontSize="14"/>
                        <TextBlock Margin="0,4,0,0" FontSize="12" FontWeight="SemiBold" Foreground="#10B981" Text="{Binding CurrentCashBalance, StringFormat='رصيد الخزينة: {0:N2}'}" />
                    </StackPanel>
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="الملاحظات" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="4" Text="{Binding FormItem.Notes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                </Grid>

                <DockPanel>
                    <WrapPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Content="➕ دفعة جديدة" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Command="{Binding AddNewCommand}"/>
                        <Button Margin="0,0,10,0" Padding="16,10"
                                        Command="{Binding PrintReceiptCommand}"
                                        Content="🖨️ طباعة الفاتورة">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SecondaryButton}"
                                               TargetType="Button">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Setter Property="Background" Value="#3B82F6" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedItem}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="✏️ تـعـديل" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Command="{Binding EditSelectedCommand}"/>
                        <Button Content="💾 حـفـظ" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Command="{Binding SaveCommand}" IsEnabled="{Binding IsEditing}"/>
                        <Button Content="🗑️ حـذف" Style="{StaticResource DangerButton}" Margin="0,0,8,8" Command="{Binding DeleteCommand}"/>
                        <Button Content="✖ إلـغـاء" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Command="{Binding CancelEditCommand}" IsEnabled="{Binding IsEditing}"/>
                    </WrapPanel>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#10B981" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="24,0,0,0"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
