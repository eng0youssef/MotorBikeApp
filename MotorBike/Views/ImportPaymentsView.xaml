<UserControl x:Class="MotorBike.Views.ImportPaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MotorBike.Converters"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Loaded="OnLoaded" FlowDirection="RightToLeft">
    <UserControl.Resources>
        <conv:IdToNameConverter x:Key="IdToNameConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24,24,24,16">
            <StackPanel Orientation="Horizontal">
                <Border Background="#FEF3C7" CornerRadius="12" Padding="14,12" Margin="0,0,16,0">
                    <TextBlock Text="💰" FontSize="26" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="مدفوعات الاستيراد" FontSize="22" FontWeight="Bold" Foreground="#1E293B"/>
                    <TextBlock Text="إدارة الدفعات المسددة واختيار الفواتير الخاصة بها" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- DataGrid Card (Master) -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Margin="24,0,24,16" BorderBrush="#E2E8F0" BorderThickness="1" ClipToBounds="True">
            <Border.Effect><DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="12" ShadowDepth="4" Direction="270"/></Border.Effect>
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" RowHeight="44"
                      ColumnHeaderHeight="48" AlternatingRowBackground="#F8FAFC" Background="White" Foreground="#1E293B" FontSize="14" HorizontalGridLinesBrush="#F1F5F9">
                <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F8FAFC"/><Setter Property="Foreground" Value="#475569"/>
                    <Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Padding" Value="16,12"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/><Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style></DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#DBEAFE"/><Setter Property="Foreground" Value="#1D4ED8"/></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#EFF6FF"/></Trigger>
                    </Style.Triggers>
                </Style></DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم الدفعة" Binding="{Binding PayId}" Width="100"/>
                    <DataGridTextColumn Header="تاريخ الدفع" Binding="{Binding PayDate, StringFormat=\{0:yyyy/MM/dd\}}" Width="100"/>
                    <DataGridTemplateColumn Header="المورد" Width="200"><DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <TextBlock VerticalAlignment="Center" Padding="4,0"><TextBlock.Text>
                            <MultiBinding Converter="{StaticResource IdToNameConverter}" ConverterParameter="SuppId,SuppName">
                                <Binding Path="SuppId"/><Binding Path="DataContext.Suppliers" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding></TextBlock.Text></TextBlock>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="الخزنة" Width="150"><DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <TextBlock VerticalAlignment="Center" Padding="4,0"><TextBlock.Text>
                            <MultiBinding Converter="{StaticResource IdToNameConverter}" ConverterParameter="CashId,CashName">
                                <Binding Path="CashId"/><Binding Path="DataContext.CashList" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding></TextBlock.Text></TextBlock>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                    <DataGridTextColumn Header="المبلغ" Binding="{Binding PayMoney, StringFormat=\{0:N2\}}" Width="120" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="الفاتورة (إن وجدت)" Binding="{Binding InvId}" Width="150"/>
                    <DataGridTextColumn Header="الملاحظات" Binding="{Binding Notes}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Form Card (Detail) -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="24" Margin="24,0,24,24" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect><DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="12" ShadowDepth="4" Direction="270"/></Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,24" IsEnabled="{Binding IsEditing}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/><ColumnDefinition Width="*"/><ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="80"/><ColumnDefinition Width="*"/><ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="80"/><ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="رقم الدفعة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FormItem.PayId}" Style="{StaticResource FormTextBox}" IsReadOnly="True" Background="#F1F5F9" Foreground="#94A3B8"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="تاريخ الدفع" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <DatePicker Grid.Row="0" Grid.Column="4" SelectedDate="{Binding FormItem.PayDate}" Padding="10,4" FontSize="14"/>
                    <TextBlock Grid.Row="0" Grid.Column="6" Text="المبلغ المدفوع" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="7" Text="{Binding FormItem.PayMoney, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="المورد" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Suppliers}" DisplayMemberPath="SuppName" SelectedValuePath="SuppId" SelectedValue="{Binding FormItem.SuppId}" Padding="10,6" FontSize="14" IsEditable="True"/>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="الخزنة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="2" Grid.Column="4" ItemsSource="{Binding CashList}" DisplayMemberPath="CashName" SelectedValuePath="CashId" SelectedValue="{Binding FormItem.CashId}" Padding="10,6" FontSize="14"/>
                    <TextBlock Grid.Row="2" Grid.Column="6" Text="العملة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="2" Grid.Column="7" Orientation="Horizontal">
                        <ComboBox ItemsSource="{Binding Omlas}" DisplayMemberPath="OmlaName" SelectedValuePath="OmlaId" SelectedValue="{Binding FormItem.OmlaId}" Padding="10,6" FontSize="14" Width="100" Margin="0,0,12,0"/>
                        <TextBlock Text="السعر:" VerticalAlignment="Center" Foreground="#64748B" Margin="0,0,8,0"/>
                        <TextBox Text="{Binding FormItem.OmlaRate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}" Width="80"/>
                    </StackPanel>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="رقم الفاتورة" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Invoices}" DisplayMemberPath="InvId" SelectedValuePath="InvId" SelectedValue="{Binding FormItem.InvId}" Padding="10,6" FontSize="14" IsEditable="True"/>
                    <TextBlock Grid.Row="4" Grid.Column="3" Text="الملاحظات" Style="{StaticResource FormLabel}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="4" Grid.ColumnSpan="4" Text="{Binding FormItem.Notes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBox}"/>
                </Grid>

                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Content="➕ دفعة جديدة" Style="{StaticResource PrimaryButton}" Command="{Binding AddNewCommand}"/>
                        <Button Content="✏️ تـعـديل" Style="{StaticResource SecondaryButton}" Command="{Binding EditSelectedCommand}"/>
                        <Button Content="💾 حـفـظ" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" IsEnabled="{Binding IsEditing}"/>
                        <Button Content="🗑️ حـذف" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}"/>
                        <Button Content="✖ إلـغـاء" Style="{StaticResource SecondaryButton}" Command="{Binding CancelEditCommand}" IsEnabled="{Binding IsEditing}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#10B981" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="24,0,0,0"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
